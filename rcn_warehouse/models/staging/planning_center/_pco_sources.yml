version: 2

sources:
  - name: planning_center
    database: resonate-data-warehouse-442601
    schema: planning_center
    tables:
      - name: people
        description: PCO people records (JSON API format)
      - name: campuses
        description: PCO campus/location records
      - name: households
        description: PCO household groupings
      - name: check_ins
        description: PCO check-in records for events and services
      - name: check_in_events
        description: PCO check-in event definitions
      - name: headcounts
        description: PCO headcount records for service plan times
      - name: service_types
        description: PCO service type definitions
      - name: emails
        description: PCO email addresses linked to people
      - name: phone_numbers
        description: PCO phone numbers linked to people
      - name: addresses
        description: PCO physical addresses linked to people
      - name: field_data
        description: PCO custom field values (baptism, demographic, etc.)
      - name: field_definitions
        description: PCO custom field definitions (lookup table for field_data)
      - name: plans
        description: PCO service plans (individual worship services)
      - name: service_teams
        description: PCO team definitions within the services module
