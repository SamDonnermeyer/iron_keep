name: 'rcn_warehouse'
version: '1.0.0'

profile: 'rcn_warehouse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  rcn_warehouse:
    staging:
      +materialized: view
      planning_center:
        +schema: staging
      engage_spaces:
        +schema: staging
    core:
      +materialized: table
      +schema: core
    marts:
      +materialized: table
      +schema: marts
